- [Directory Structure](#directory-structure)
- [Introduction](#introduction)
- [Installation](#installation)
---
# Directory Structure
```bash
Pandas/                  # mình dùng thư mục này để xem kiến thức về pandas
├── IO.md                # mình dùng file này để thao tác đọc (lấy) dữ liệu và xuất dữ liệu
├── Practices.md         # mình dùng file này để xem code mẫu, bài tập
├── Process.md           # mình dùng file này để thao tác xử lý
└── Transformation.md    # mình dùng file này để thao tác biến đổi cấu trúc dữ liệu
```
# Introduction
```bash
- Thường dùng để xử lý dữ liệu. Pandas kế thừa numpy tức là nó có thể sử dụng hầu hết các hàm trong numpy
```
# Installation
```bash
1. pip install pandas / conda install pandas.
```
