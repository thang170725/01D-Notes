- [Introduction](#introduction)
- [Installation](#installation)
---
# Introduction
```bash
- Thư viện Requests trong Python dùng để gửi HTTP request (GET, POST, PUT, DELETE…) đến server — thường dùng để:
    + Gọi API (REST API)
    + Crawl dữ liệu web
    + Gửi/nhận dữ liệu giữa client ↔ server
```
# Installation
```bash
pip install requests
```