# Directory structure
```bash
Time/                 # Mình dùng thư mục này để xem các thư viện có sẵn không cần tải 
├── Datetime/         # Mình dùng thư mục này để thao tác với thư viện datetime
├── Time/             # Mình dùng thư mục này để định nghĩa kiểu dữ liệu và cấu trúc chứa dữ liệu
├── Base.md           # Mình dùng file này để xem kiến thức và tiện ích
└── Practices.md      # Mình dùng file này để xem code mẫu, bài tập
```
