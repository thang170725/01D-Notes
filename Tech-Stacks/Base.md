- [Directory Structure](#directory-structure)
---
# Directory Structure
```bash
Stack           # mình dùng thư mục này để xem tất cả kiến thức về công nghệ trong ngành CNTT
├── Databases   # mình dùng thư mục này để xem kiến thức về db
├── DevOps      # mình dùng thư mục này để xem kiến thức về dev-ops
├── Frameworks  # mình dùng thư mục này để xem kiến thức về framework        
├── Graplics    # mình dùng thư mục này để xem kiến thức về đồ họa
└── Languages   # mình dùng thư mục này để xem kiến thức về ngôn ngữ lập trình
```