- [Cấu trúc thư mục](#cấu-trúc-thư-mục)
- [Cách mở khóa Office 365](#cách-mở-khóa-office-365)
---
# Directory Structure
```bash
Terminal/              # Mình dùng file này để xem các lệnh terminal
├── File_Directory.md  # Mình dùng file này để thao tác File & Thư mục
├── System.md          # Mình dùng file này để thao tác với hệ thống (phần cứng, phần mềm)
└── Software.md        # Mình dùng file này để thao tác với phần mềm cài ngoài (cài, xóa, v.v)
```