- [Directory structure](#directory-structure)
- [Introduction](#introduction)
---
# Directory structure
```bash
Threading/                    # Mình dùng thư mục này để xem các thư viện có sẵn không cần tải 
├── Base.md                   # Mình dùng thư mục này để xem các thư viện liên quan đến hệ thống
├── Practices.md  # Mình dùng thư mục này để định nghĩa kiểu dữ liệu và cấu trúc chứa dữ liệu
└── Process.md                  # Mình dùng thư mục này để tối ưu chạy chương trình, xử lý thời gian
```
# Introduction
```bash
- Threading không giúp xử lý logic nhanh hơn nó chỉ làm nhiều thứ diễn ra cùng lúc.
- Nó chỉ giúp ích cho một số trường hợp.
    + tách luồng đọc, hiển thị và xử lý logic để FPS mượt hơn != xử lý nhanh hơn
```