- [Directory Structure](#directory-structure)
- [Introduction](#introduction)
---
# Directory Structure
```bash
Vispy/           # mình dùng thư mục này để xem kiến thức về Vispy
├── Base.md      # mình dùng file này để xem kiến thức cơ bản và tiện ích
├── Practices.md # mình dùng file này để xem  code mẫu và bài tập
└── Process.md   # mình dùng file này để thao tác xử lý
```
# Introduction
```bash
- Một thư viện mạnh mẽ cho việc trực quan hóa dữ liệu 2D và 3D hiệu suất cao bằng Python, sử dụng OpenGL. 
- Một thư viện đồ họa hiệu năng cao. Nó không dùng CPU để vẽ từng điểm, mà gửi toàn bộ dữ liệu xuống Card đồ họa (GPU) để xử lý song song.
```