- [Introduction (tạo ID gần như không bao giờ trùng nhau)](#introduction-tạo-id-gần-như-không-bao-giờ-trùng-nhau)
---
# Introduction (tạo ID gần như không bao giờ trùng nhau)
```bash
- uuid là thư viện có sẵn (built-in)
```
