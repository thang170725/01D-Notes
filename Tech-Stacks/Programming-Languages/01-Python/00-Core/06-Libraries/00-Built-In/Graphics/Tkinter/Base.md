- [Directory Structure](#directory-structure)
---
# Directory Structure
Tkinter/       
├── [Filedialog](Filedialog.md)     ```mình dùng thư mục này để xem các thư viện có sẵn không cần tải```   
├── Data-Science/ # mình dùng thư mục này để xem  bộ công cụ dành cho dân data Science
├── Html        # mình dùng thư mục này để xem kiến thức về Html      
├── Css         # mình dùng thư mục này để xem kiến thức về Css
├── Java        # mình dùng thư mục này để xem kiến thức về Java
└── 
```
# Tải thư viện từ file requirements.txt
```bash
1. pip freeze > requirements.txt    # lấy các thư viện để ghi vào file requirements.txt
2. pip install -r requirements.txt  # tải thư viện từ file requirements 
```